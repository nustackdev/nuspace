"""Nuspace-ui python entry.

Composes nuspace-shell + specialized refs (apps_explorer, pages_renderer,
lens) into one nu.ui tree and mounts it. Runs as a plain nu.ui program
alongside kv and proxy under the running ``nu.With``.

v1 skeleton -- refs land here as they get built.
"""

from __future__ import annotations


def build_ui():
    """Return the nu tree for nuspace-ui. Placeholder for v1."""
    raise NotImplementedError("nuspace-ui tree not composed yet")
