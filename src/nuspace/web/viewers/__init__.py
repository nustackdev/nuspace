"""How a Plane is drawn. One package per viewer, named for the viewer.

A Plane names its viewer in a prop, so a viewer is a value in a slot rather
than a branch in a driver. Each one is three files sitting together: the ref it
mounts on the browser's tree, the wire vocabulary that ref speaks, and the arms
that turn one into the other.

Which viewer a Plane gets is the Plane's own business, and putting a Plane's
Cells in front of a person is the web driver's.
"""
